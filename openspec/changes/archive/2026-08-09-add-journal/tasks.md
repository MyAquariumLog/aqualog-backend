## 1. Data model & migration

- [x] 1.1 Add `AquariumJournalEntry` model to `src/models.py` (`id`, `aquarium_id` FK → `aquariums.id` `ondelete=CASCADE` indexed, `entry_at` timezone-aware datetime, `message` string with max length, `created_at`/`updated_at` via existing `_utc_now` convention)
- [x] 1.2 Generate Alembic migration with `task db-migration-new NAME="add_aquarium_journal_entries"` and review the autogenerated revision for correctness (FK, cascade, indexes)
- [x] 1.3 Run `task db-migrate` locally and confirm the migration applies cleanly

## 2. Repository

- [x] 2.1 Create `src/aquarium_journal_entry_repository.py` with `AquariumJournalEntryRepository` (`create`, `list_by_aquarium` ordered by `entry_at` ascending, `get_by_id_and_aquarium`, `update_by_id_and_aquarium` (partial field updates), `delete_by_id_and_aquarium`)
- [x] 2.2 Add `tests/test_aquarium_journal_entry_repository.py` covering create, list ordering, get-by-id (found/not-found), update (found/not-found, partial field updates, `updated_at` bump), delete (found/not-found), and cascade delete when the parent aquarium is deleted

## 3. Schemas

- [x] 3.1 Create `src/schemas/aquarium_journal_entries.py` with `CreateJournalEntryRequest` (`entry_at`, `message` non-empty/max-length validated), `UpdateJournalEntryRequest` (optional `entry_at`/`message`, both individually validated when present, at least one required), `JournalEntryResponse` (includes `updated_at`), and `JournalEntryListResponse`

## 4. Router

- [x] 4.1 Create `src/routers/aquarium_journal_entries.py` exposing `build_journal_router()` mounted at `/aquariums/{aquarium_id}/journal`, requiring `get_current_user` and verifying aquarium ownership via `AquariumRepository.get_by_id_and_owner` before any journal operation
- [x] 4.2 Implement `POST /aquariums/{aquarium_id}/journal` — normalize `entry_at` to whole-second UTC, persist, return `201` via `success_response`
- [x] 4.3 Implement `GET /aquariums/{aquarium_id}/journal` — list entries chronologically via `success_response`
- [x] 4.4 Implement `GET /aquariums/{aquarium_id}/journal/{entry_id}` — return `404` if not found on the owned aquarium
- [x] 4.5 Implement `PATCH /aquariums/{aquarium_id}/journal/{entry_id}` — reject empty payloads, normalize `entry_at` if supplied, re-validate `message` if supplied, return `404` if not found, otherwise persist and return the updated entry
- [x] 4.6 Implement `DELETE /aquariums/{aquarium_id}/journal/{entry_id}` — return `404` if not found, otherwise delete and return success
- [x] 4.7 Register `build_journal_router()` in `src/app.py` alongside the other resource routers

## 5. Router tests

- [x] 5.1 Create `tests/test_aquarium_journal_entries.py` covering: create (success, missing fields, empty/overlength message), list (chronological order, empty result), get-by-id (success, not-found), update (success — message only, entry_at only, both; empty payload rejected; invalid message rejected; not-found), delete (success, not-found), and ownership scoping (403/404 for another user's aquarium on create/list/get/update/delete)
- [x] 5.2 Verify response envelope shape (`success`, `request_id`, `data`) matches existing resource tests for all new endpoints

## 6. Verification

- [x] 6.1 Run `task lint`, `task format-check`, `task typecheck`
- [x] 6.2 Run `task test` and confirm coverage on new modules meets the project's 80% target
- [x] 6.3 Manually exercise the new endpoints via `task server` + `task token` (create, list, get, delete a journal entry) to confirm end-to-end behavior

## 7. Make entry_at optional on create (defaults to now)

- [x] 7.1 Make `CreateJournalEntryRequest.entry_at` optional (`datetime | None`); router defaults it to `datetime.now(timezone.utc)` when omitted, before normalization
- [x] 7.2 Add router tests: create without `entry_at` defaults to current time; create with explicit `entry_at` still respected
- [x] 7.3 Re-run lint/format/typecheck/tests
